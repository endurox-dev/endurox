/*--------------------------------------------------------------------------
 * This file is autogenerated from ndrx_config.h.in 
 * during the cmake configuration of your project. If you need to make changes
 * edit the original file NOT THIS FILE.
 * --------------------------------------------------------------------------*/
#ifndef _NDRX_CONFIG_H_
#define _NDRX_CONFIG_H_

/* Define to 1 if you have the <stdint.h> header file. */
#cmakedefine HAVE_STDINT_H @HAVE_STDINT_H@

#cmakedefine HAVE_GETOPT_H @HAVE_GETOPT_H@

#cmakedefine HAVE_PROGNAME @HAVE_PROGNAME@

#cmakedefine HAVE_EXPECT @HAVE_EXPECT@

#cmakedefine HAVE_SYNC @HAVE_SYNC@

#cmakedefine EX_USE_EPOLL @EX_USE_EPOLL@

#cmakedefine EX_OS_LINUX @EX_OS_LINUX@

#cmakedefine EX_OS_AIX @EX_OS_AIX@

#cmakedefine EX_OS_HPUX @EX_OS_HPUX@

#cmakedefine EX_OS_SUNOS @EX_OS_SUNOS@

#cmakedefine EX_OS_FREEBSD @EX_OS_FREEBSD@

#cmakedefine EX_OS_CYGWIN @EX_OS_CYGWIN@

#cmakedefine EX_OS_DARWIN @EX_OS_DARWIN@

/* Use emulated message queue: */
#cmakedefine EX_USE_EMQ @EX_USE_EMQ@

/* Do not use threading model in client proces monitor
 * for example on AIX 7.2 it causes core dumps...
 */
#cmakedefine EX_CPM_NO_THREADS @EX_CPM_NO_THREADS@

/* Defined if pscript is off */
#cmakedefine NDRX_DISABLEPSCRIPT @NDRX_DISABLEPSCRIPT@

/* Enduro/X memory debugging */
#cmakedefine NDRX_MEMORY_DEBUG @NDRX_MEMORY_DEBUG@

/* Object API debug - tracking */
#cmakedefine NDRX_OAPI_DEBUG @NDRX_OAPI_DEBUG@

/* Use FS based registry for mq */
#cmakedefine USE_FS_REGISTRY @USE_FS_REGISTRY@

/* The size of `long', as computed by sizeof. */
#define SIZEOF_INT @SIZEOF_INT@

#define NDRX_VERSION   "@NDRX_VERSION_STR@"
#define NDRX_BUILD_OS_NAME "@CMAKE_OS_NAME@"

/* PThread mode for Solaris */
#define _POSIX_PTHREAD_SEMANTICS

#if defined(_WIN32) || defined(__WIN32__) || defined(__CYGWIN__)

#if defined(atmi_EXPORTS) || defined(atmiclt_EXPORTS) || defined(atmisrv_EXPORTS) || defined(atmisrvinteg_EXPORTS) || defined(atmisrvnomain_EXPORTS) || defined(exnet_EXPORTS) || defined(ndrxxaorad_EXPORTS) || defined(ndrxxaoras_EXPORTS) || defined(ndrxxaqdisk_EXPORTS) || defined(ndrxxaqdiskd_EXPORTS) || defined(ndrxxaqdisks_EXPORTS) || defined(netproto_EXPORTS) || defined(nstd_EXPORTS) || defined(ubf_EXPORTS)
        /* We are building this library */
#      define NDRX_API __declspec(dllexport)
#else
        /* We are using this library */
#       define NDRX_API __declspec(dllimport)
#endif

#else

#       define NDRX_API

#endif

#endif
